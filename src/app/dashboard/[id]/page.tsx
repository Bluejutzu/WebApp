import UserAPI from "@/components/dashboard/UserAPI";
import React from "react";

const page = () => {
    return (
        <div className="container mx-auto flex items-start justify-center py-4 px-6">
            <UserAPI />
        </div>
    );
};

export default page;
